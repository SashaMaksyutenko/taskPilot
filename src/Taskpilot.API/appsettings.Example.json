{
  "//": "Config template. The real connection string and secrets are NOT stored here.",
  "//connection": "Set the DB connection string via User Secrets (local dev) or an environment variable (production):",
  "//command": "dotnet user-secrets set \"ConnectionStrings:DefaultConnection\" \"Host=localhost;Port=5433;Database=taskpilot;Username=postgres;Password=YOUR_PASSWORD\"",
  "//jwt": "Also set via User Secrets: Jwt:Key, Jwt:Issuer, Jwt:Audience",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
